spring:
  datasource:
    username: test
    password: test
  profiles:
    active: test
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: delivery
        show_sql: true
        format_sql: true